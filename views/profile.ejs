<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile - ReWear</title>
    <link rel="stylesheet" href="/styles/HomePage.css" />
    <style>
      .profile-header {
        display: flex;
        align-items: center;
        gap: 1.2rem;
        padding: 2rem;
        background: #fff;
        box-shadow: 0 2px 12px #a6c1ee33;
      }
      .profile-avatar {
        width: 64px;
        height: 64px;
        border-radius: 50%;
        background: #805ad5;
        color: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2.5rem;
        font-weight: bold;
      }
      .profile-details {
        font-size: 1.2rem;
        color: #805ad5;
      }
    </style>
  </head>
  <body>
    <main style="padding:2rem; max-width:700px; margin:auto;">
      <a href="/dashboard" style="color:#805ad5; text-decoration:underline; font-size:1rem; display:inline-block; margin-bottom:2rem;">&larr; Back to Dashboard</a>
      <section style="margin-bottom:2rem;">
        <h2>Welcome, <%= username %>!</h2>
        <p><strong>Email:</strong> <%= email %></p>
        <p><strong>Points:</strong> <%= points %></p>
      </section>
      <section style="margin-bottom:2rem;">
        <h3>My Listings</h3>
        <ul>
          <% if (typeof listings !== 'undefined' && listings.length === 0) { %>
            <li>No listings yet.</li>
          <% } else if (typeof listings !== 'undefined') { listings.forEach(function(listing) { %>
            <li><%= listing %></li>
          <% }); } %>
        </ul>
      </section>
      <section style="margin-bottom:2rem;">
        <h3>Purchased Dresses</h3>
        <ul>
          <% if (typeof purchases !== 'undefined' && purchases.length === 0) { %>
            <li>No purchases yet.</li>
          <% } else if (typeof purchases !== 'undefined') { purchases.forEach(function(purchase) { %>
            <li><%= purchase %></li>
          <% }); } %>
        </ul>
      </section>
    </main>
  </body>
</html>
