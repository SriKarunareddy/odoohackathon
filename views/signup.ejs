<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ReWear - Signup</title>
    <link rel="stylesheet" href="/styles/HomePage.css" />
    <style>
      .signup-container {
        min-height: 100vh;
        width: 100vw;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(120deg, #fbc2eb 0%, #a6c1ee 100%);
        gap: 3rem;
        padding: 2rem 0;
      }
      .signup-box {
        background: #fff;
        border-radius: 18px;
        box-shadow: 0 8px 32px #805ad555, 0 2px 12px #38b2ac33;
        padding: 2.5rem 2.5rem 2rem 2.5rem;
        min-width: 320px;
        max-width: 90vw;
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
      }
      .signup-title {
        font-family: 'Montserrat', 'Segoe UI', Arial, sans-serif;
        font-size: 1.4rem;
        font-weight: 700;
        color: #38b2ac;
        margin-bottom: 1.2rem;
        letter-spacing: 1px;
      }
      .signup-form {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }
      .signup-input {
        padding: 0.7rem 1rem;
        border-radius: 7px;
        border: 1.5px solid #a6c1ee;
        font-size: 1rem;
        outline: none;
        transition: border 0.2s;
      }
      .signup-input:focus {
        border: 2px solid #38b2ac;
      }
      .signup-submit {
        background: linear-gradient(90deg, #38b2ac 0%, #fbc2eb 100%);
        color: #fff;
        border: none;
        border-radius: 7px;
        padding: 0.7rem 0;
        font-size: 1.1rem;
        font-weight: 700;
        cursor: pointer;
        box-shadow: 0 2px 12px #fbc2eb55;
        transition: background 0.2s, color 0.2s, box-shadow 0.2s;
      }
      .signup-submit:hover {
        background: linear-gradient(90deg, #fbc2eb 0%, #38b2ac 100%);
        color: #2d3748;
      }
      @media (max-width: 900px) {
        .signup-container {
          flex-direction: column;
          gap: 2rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="signup-container">
      <div class="signup-box">
        <h2 class="signup-title">User Signup</h2>
        <form class="signup-form" action="/signup/user" method="POST">
          <input type="text" name="username" class="signup-input" placeholder="Username" required />
          <input type="email" name="email" class="signup-input" placeholder="Mail ID" required />
          <input type="password" name="password" class="signup-input" placeholder="Password" required />
          <input type="text" name="location" class="signup-input" placeholder="Location" required />
          <button type="submit" class="signup-submit">Create</button>
        </form>
      </div>
    </div>
</body>

</html>
